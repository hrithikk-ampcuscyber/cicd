services:
  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8000:8000"
    environment:
      ENVIRONMENT: local
      APP_VERSION: local-dev
    restart: unless-stopped
